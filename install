#!/bin/bash

cd "$(dirname ${BASH_SOURCE[0]})"

FILES=(
    'bash.d'
    'bash_profile'
    'vimrc'
)

for FILE in "${FILES[@]}"; do
    DEST="$HOME/.$FILE"
    rm -rf "$DEST"
    ln -sf "$PWD/$FILE" "$DEST"
done
