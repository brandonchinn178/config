#compdef git-bb

function _git_bb {
    emulate -L zsh
    local flags=(
        list
        base
        deps
        display
        rename
        set-base
        add-deps
        rm-deps
        clear
    )
    __gitcomp "${flags}"

    __gitcomp_direct "$(__git_heads)"
}
